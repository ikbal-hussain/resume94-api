{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "builds": [
    {
      "src": "api/index.js",
      "use": "@vercel/node",
      "config": {
        "_comment": "swagger-ui serves its bundle from disk via express.static. Vercel traces JS imports only, so these assets must be requested explicitly or /api/docs loads with no scripts.",
        "includeFiles": "node_modules/swagger-ui-dist/**"
      }
    }
  ],
  "routes": [{ "src": "/(.*)", "dest": "/api/index.js" }]
}
